const Header = ({ title }) => (
  <header className="text-center py-6 shadow-sm bg-white">
    <h1 className="text-3xl md:text-4xl font-serif font-bold text-gray-800">{title}</h1>
  </header>
);

export default Header;
